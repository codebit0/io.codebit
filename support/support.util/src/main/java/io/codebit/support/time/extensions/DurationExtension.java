package io.codebit.support.time.extensions;

import java.time.Duration;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public abstract class DurationExtension
{
//	public static String format(Duration duration, CharSequence format)
//	{
//		LocalDateTime d = LocalDateTime.of(0, 0, 0, 0, 0);
//		d.plus(duration);
//		LocalTime t = LocalTime.MIDNIGHT.plus(duration);
//		return DateTimeFormatter.ofPattern(format.toString()).format(t);
//	}
}
